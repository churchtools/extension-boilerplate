{
    "name": "extension-boilerplate",
    "private": true,
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "build:simple": "VITE_BUILD_MODE=simple npm run build",
        "build:advanced": "VITE_BUILD_MODE=advanced npm run build",
        "preview": "vite preview",
        "deploy": "npm run build && node scripts/package.js",
        "deploy:simple": "VITE_BUILD_MODE=simple npm run build && node scripts/package.js",
        "deploy:advanced": "VITE_BUILD_MODE=advanced npm run build && node scripts/package.js"
    },
    "dependencies": {
        "@churchtools/extension-points": "file:../churchtools-extension-points"
    },
    "devDependencies": {
        "@churchtools/churchtools-client": "^1.4.0",
        "@types/node": "^24.3.0",
        "typescript": "^5.9.2",
        "vite": "^7.1.2"
    }
}
